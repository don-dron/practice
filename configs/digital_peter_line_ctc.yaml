# См. методологический профиль §4.1 и configs/baselines/4_01_line_crnn_ctc_digital_peter.yaml .
# Серверный бюджет: configs/baselines/gpu_server_line_train_budget.yaml после этой цепочки.

methodology_profile:
  report_section: "4.1"
  short_name: line_crnn_bilstm_ctc_digital_peter
  status: trainable

project:
  seed: 42
  device: "cuda"

training:
  experiment_name: "dpeter_crnn_ctc"
  checkpoint_dir: "training/checkpoints"

infer:
  checkpoint: "training/checkpoints/latest.pt"
