# §4.1 — строковое распознавание CNN+BiLSTM+CTC; данные те же что в default (Yenisei). Бюджет batch/VRAM — из default.yaml.
# Кластер: добавьте -c configs/baselines/gpu_server_line_train_budget.yaml

methodology_profile:
  report_section: "4.1"
  short_name: line_crnn_bilstm_ctc_digital_peter
  status: trainable

project:
  seed: 42
  device: "cuda"

training:
  epochs: 2
  experiment_name: "dpeter_crnn_ctc"
  checkpoint_dir: "training/checkpoints"

infer:
  checkpoint: "training/checkpoints/latest.pt"
