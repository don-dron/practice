# Перекрытие «сопоставимого бюджета» для строковых профилей (§5.1):
# задаёт общие параметры данных и обучения, если их не переопределит более поздний YAML в цепочке.
# Рекомендуемая цепочка: default.yaml → этот файл → профиль методологии configs/baselines/4_xx_*.yaml .

data:
  max_width: 900
  val_fraction: 0.03
  num_workers: 0

training:
  batch_size: 8
  epochs: 20
  lr: 0.001
