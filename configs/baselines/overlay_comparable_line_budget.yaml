# Общая часть методологических прогонов §5.1 без «утопления» default по памяти.
# Серверный batch/max_width/workers см. configs/baselines/gpu_server_line_train_budget.yaml (передавать третьим).

data:
  val_fraction: 0.03

training:
  epochs: 5
  lr: 0.05
