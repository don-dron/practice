# Алиас методологии: то же объединение трёх корпусов, что уже в configs/default.yaml (data.sources).
# Дополнительный YAML не обязателен — достаточно: htr-train --config configs/default.yaml
#
# ROO: распакуйте страницы (pages-img-plaintext); при отсутствии пар источник с optional пропускается с предупреждением.

methodology_profile:
  report_section: "multi"
  short_name: yenisei_peter_oldorth_pages
  status: trainable
