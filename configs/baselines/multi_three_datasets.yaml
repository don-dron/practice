# Алиас методологии: то же объединение двух COCO-корпусов, что в configs/default.yaml (data.sources).
# Имя файла историческое (раньше был третий источник page_txt_pairs). Дополнительный YAML не обязателен:
#   htr-train --config configs/default.yaml

methodology_profile:
  report_section: "multi"
  short_name: yenisei_peter_coco_lines
  status: trainable
